export const addModule = (moduleCode: string, moduleName: string) => {
    console.log("Adding module " + moduleCode + " " + moduleName);
    return;
};

export const addTimeSlot = (moduleCode: string, selectedDay: string, selectedTime: string, length: number, room: string) => {
    console.log("Adding time slot " + moduleCode + " " + selectedDay + " " + selectedTime + " " + length + " " + room);
    return;
};
